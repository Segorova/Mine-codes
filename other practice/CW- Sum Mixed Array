function sumMix(x){
return x.map(el => +el).reduce((acc,el) => acc + el)
}
*************************
function sumMix(x){
  let result = 0;
  for (let n of x) {
    result += parseInt(n);
  }
  return result;
}
