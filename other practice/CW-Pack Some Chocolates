function makeChocolates(small, big, goal) {
 for (let i = 0; i <= small; i++){
    for (let j = 0; j <= big; j++){
             console.log([i, j]);
      if (goal === ((j * 5) + (i * 2)))
        return i;
    }
  }
  return -1;
}
console.log(makeChocolates(4, 1, 13)); // 4
