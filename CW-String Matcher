function isMatching(string, str1, str2) {
let str = (str1+str2).toLowerCase().replace(/\s/g, '').split('').sort().join('')
return str === string.toLowerCase().replace(/\s/g, '').split('').sort().join('')
}
