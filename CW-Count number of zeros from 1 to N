function countZeros(n) {
  let str = ''
   for (let i = 10; i <= n; i++) {
     str += i
    }
  return str.split('').filter(el => el === '0').length  // return str.replace(/[^0]/g, '').length;
}

************************************

