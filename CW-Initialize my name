function initializeNames(name){
  let str = name.split(' ')
  let res = str[0] + ' '+ str.slice(1,-1).map(el => el[0] + '. ') + str[str.length-1]
 return str.length === 1 ? name : res.replace(/,/g, '')
}
