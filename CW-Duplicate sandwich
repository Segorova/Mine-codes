function duplicateSandwich(arr) {
  let res = []
     for (let i = 0; i < arr.length; i++) {       
      if (arr.indexOf(arr[i]) !== arr.lastIndexOf(arr[i])) {
        res = arr.slice(i+1, arr.lastIndexOf(arr[i]))
        break
   }}
return res
  }
