function redarr(arr) {
  let arr2 = arr.filter((el, i) => arr.indexOf(el) === i).sort()
  let obj = {}
     for (let i = 0; i < arr2.length; i++) {
       obj[i] = arr2[i]
     }
return obj
  }
  
  
  
