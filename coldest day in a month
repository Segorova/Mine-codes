function theColdestDay(arr){
  let min = arr[0], num = 0
   for (let i = 0; i < arr.length; i++) { 
      if (arr[i] <= min) {
        min = arr[i]
        num = i
      }
   }     
  return `April ${num+1} was the coldest day of the month: it was ${min} degrees.`
  }
